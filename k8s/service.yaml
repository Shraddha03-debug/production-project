apiVersion: v1
kind: Service
metadata:
  name: devops-demo-svc
  namespace: devops-demo
spec:
  type: ClusterIP
  selector:
    app: devops-demo
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
